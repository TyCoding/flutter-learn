import 'package:flutter/material.dart';

class ScrollControllerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ScrollController Page'),
      ),
      body: Center(
        child: Column(
          children: <Widget>[
            Text('XX')
          ],
        ),
      ),
    );
  }
}
